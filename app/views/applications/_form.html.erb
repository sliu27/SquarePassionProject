<%= form_with(model: application, local: true) do |form| %>
  <% if application.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(application.errors.count, "error") %> prohibited this application from being saved:</h2>

      <ul>
      <% application.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user %>
    <%= form.text_field :user, id: :application_user %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.text_field :status, id: :application_status %>
  </div>

  <div class="field">
    <%= form.label :shelter %>
    <%= form.text_field :shelter, id: :application_shelter %>
  </div>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_field :address, id: :application_address %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
